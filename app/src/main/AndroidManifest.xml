<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lozyloop.pawtravel">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:largeHeap="true">
        <activity android:name=".Screen.Loading.LoadingActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Screen.Register.RegisterActivity">

        </activity>
        <activity android:name=".Screen.Login.SigninActivity" />
        <activity android:name=".Screen.Register.CreateActivity" >

        </activity>
        <activity android:name=".Screen.Reset.ResetActivity" >

        </activity>
        <activity android:name=".Screen.Handle.TravelActivity">

        </activity>

        <activity android:name=".Screen.ItemTravel.SlideImageActivity">

        </activity>
    </application>

</manifest>